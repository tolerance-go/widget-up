{
  "name": "react16_css-modules",
  "version": "0.0.0",
  "description": "",
  "module": "dist/esm/index.js",
  "typings": "dist/types/index.d.ts",
  "main": "dist/cjs/index.js",
  "type": "module",
  "scripts": {
    "build": "wup build",
    "dev": "wup dev",
    "prepublishOnly": "npm run build",
    "postversion": "git add package.json && git commit -m \"release: $(node -p \\\"'${npm_package_name} v' + require('./package.json').version\\\")\" && git tag v$(node -p \"require('./package.json').version\") && git push && git push --tags && npm publish && cnpm sync"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/runtime": "^7.24.4",
    "react": "16",
    "react-dom": "16"
  },
  "devDependencies": {
    "cnpm": "^9.4.0",
    "typescript": "^5.4.5",
    "widget-up": "workspace:^"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
