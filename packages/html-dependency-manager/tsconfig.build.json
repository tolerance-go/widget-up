{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.*", // 排除所有命名为 *.test.* 的文件
    "**/*.test.ts", // 可以指定具体的后缀，如果测试文件有统一的后缀
    "**/__tests__/**" // 如果你将测试文件放在 __tests__ 文件夹
  ]
}
