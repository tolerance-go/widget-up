{
  "description": "",
  "scripts": {
    "clean": "git clean -fdX",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "lerna:publish": "npm run prepublishAll && lerna publish",
    "prepublishAll": "pnpm -r run prepublishAll",
    "setupAll": "pnpm i && pnpm build:base && pnpm i && pnpm build",
    "build": "pnpm -r run build",
    "build:base": "pnpm -r --filter widget-up-utils --filter widget-up --filter widget-up-core run build",
    "test": "pnpm -r --filter widget-up-utils --filter widget-up run test",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "conventional-changelog-cli": "^4.1.0",
    "conventional-changelog-conventionalcommits": "^7.0.2",
    "husky": "^9.0.11",
    "lerna": "^8.1.2",
    "prettier": "^2.8.8"
  }
}
